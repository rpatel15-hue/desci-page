import { Flex, FlexProps, Spacer } from '@chakra-ui/react'

export const ProjectList = (props: FlexProps) => (
  <Flex
    justifyContent="center"
    alignItems="top"
    height="100px"
    minWidth="400px"
    bgGradient="linear(to-l, heroGradientStart, heroGradientEnd)"
    bgClip="text"
  >
  </Flex>
)
